import React from 'react';
import AgentConfiguration from './AgentConfiguration';

const AgentTraining = ({ currentOrg }) => {
  // Comprehensive agent configuration interface - separate from Settings
  // This focuses on configuring individual agent behaviors, models, and prompts
  return <AgentConfiguration currentOrg={currentOrg} />;
};

export default AgentTraining;

export default AgentTraining;